# Dependencies actually used by this repo's scripts and notebooks.
# (Previously this file was a full `conda list` dump of an Anaconda base env;
# that triggered ~115 stale Dependabot alerts for packages never used here.)
#
# Install with:  pip install -r requirements.txt   (or use uv / conda)
numpy
scipy
pandas
matplotlib
seaborn
ipython
jupyter
